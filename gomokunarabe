
result = "D"


for i in range(5):

    bord = input().strip()
    pivot = bord[0]
    count = 0
    for stone in bord:
        if stone != "." and stone == pivot:
            count += 1
        else:
            break
        
    if count == 5:
        result = pivot

print(result)        
