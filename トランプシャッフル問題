deck = [f"{s}_{n}" for s in ["S","H","D","C"] for n in range(1,14)]

def shuffle(deck):
    new = []
    top = deck[:26]
    bottom = deck[26:]
    for i in range(26):
        new.append(top[i])
        new.append(bottom[i])
    return new
    
K = int(input())

for _ in range(K):
    deck = shuffle(deck)
    
for card in deck:
    print(card)
