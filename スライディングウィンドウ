N, K = map(int,input().split())
pi = [int(input()) for _ in range(N)]

pi2 = pi + pi
current_sum = sum(pi2[:K])
max_sum = current_sum
for i in range(1,N):
    current_sum = current_sum - pi2[i-1] + pi2[i+K-1]
    max_sum = max(max_sum,current_sum)
    
print(max_sum)    
    
